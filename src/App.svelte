<script lang="ts">
  // Bootstrap placeholder — replaced by the real app shell next.
  const v = '0.1.0';
</script>

<main class="boot wrap">
  <span class="kicker">Karakuri · からくり</span>
  <h1>計算を、からくりとして学ぶ。</h1>
  <p>Vite + Svelte 5 + TypeScript の足場が起動しました（v{v}）。論理回路シミュレータを組み込みます。</p>
</main>

<style>
  .boot { padding-block: 18vh 8vh; }
  .boot h1 { font-size: var(--step-4); margin-top: var(--sp-4); }
  .boot p { color: var(--paper-2); margin-top: var(--sp-4); font-family: var(--font-mono); font-size: var(--step--1); }
</style>
